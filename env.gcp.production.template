# =============================================================================
# GCP 本番用環境変数（docker-compose.gcp.yml） lots-of-love.top
# =============================================================================
# DB: ユーザー名・DB 名は Cloud SQL の「ユーザー」「データベース」に表示されているとおり（デフォルトは postgres / postgres）。
# DB_PASSWORD は「ユーザー」で postgres に設定したパスワードと一致させること。
# DB_HOST はコンソール「接続」のパブリック IP と一致するか確認すること（下記は例）。
# この内容を配布・公開する場合は、秘密鍵・DB パスワードは必ず再生成すること。
#
# VM 例: scp 本ファイル → /opt/my_ads_platform/.env
# =============================================================================

# ----------------------------------------------
# Django
# ----------------------------------------------
SECRET_KEY=2NTkLiPQZHDvf+rw0QoGse2DP2utiOceHEtjPMX/FD86LtXKIfBRvSmGsLo6UMXC
DEBUG=False
ALLOWED_HOSTS=lots-of-love.top,www.lots-of-love.top

# ----------------------------------------------
# Cloud SQL（PostgreSQL）
# ----------------------------------------------
DB_ENGINE=django.db.backends.postgresql
DB_NAME=postgres
DB_USER=postgres
# 次行: Cloud SQL「ユーザー」で postgres にパスワードを設定したうえで、同じ文字列を書く（空のままでは接続できない）。
DB_PASSWORD=hA55^(5L9Ap@GcsZ
DB_HOST=34.180.110.123
DB_PORT=5432
DB_SSLMODE=require

# ----------------------------------------------
# Redis（compose 内ホスト名 redis）
# ----------------------------------------------
REDIS_URL=redis://redis:6379/0

# ----------------------------------------------
# CORS / フロント
# ----------------------------------------------
CORS_ALLOWED_ORIGINS=https://lots-of-love.top,https://www.lots-of-love.top
FRONTEND_URL=https://lots-of-love.top

# ----------------------------------------------
# JWT
# ----------------------------------------------
JWT_SECRET_KEY=68ChAEcxLfKBovFKCaAp7WuPR62pXh4Un1ZsXJAIFYRffGYqO5L3PY7rZCljTaLI
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ----------------------------------------------
# Meta / Box（利用時のみコンソールの値を設定）
# ----------------------------------------------
META_APP_ID=
META_APP_SECRET=
META_ACCESS_TOKEN=
BOX_CLIENT_ID=
BOX_CLIENT_SECRET=
BOX_REDIRECT_URI=https://lots-of-love.top/api/accounts/box-accounts/oauth_callback/

# ----------------------------------------------
# 任意
# ----------------------------------------------
SENTRY_DSN=
